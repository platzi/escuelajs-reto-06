@import '../fonts'
@import '../vars'

.button-map
  background $green
  color $white
  font-size 1em
  font-family $font-lato
  border 0
  padding 5px 15px
  border-radius 20px
  line-height 20px
  cursor pointer
  transition all .5s ease-in-out
  &:hover
    transform scale(1.1)